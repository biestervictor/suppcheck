apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: suppcheck-cert-external-secret
  namespace: {{ .Values.suppcheck.nameSpace }}
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: {{ .Values.secretstore.name }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.suppcheck.secrets }}
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  data:
    - secretKey: tls.crt
      remoteRef:
        key: {{ .Values.suppcheck.tls.certKey | default "tls-crt" | quote }}
    - secretKey: tls.key
      remoteRef:
        key: {{ .Values.suppcheck.tls.keyKey | default "tls-key" | quote }}


