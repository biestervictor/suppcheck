## MongoDB Express configuration
mongoexpress:
  ingress:
    host: mongoexpress.dev.biester.vip

## general
ingress:
  className: traefik
volume:
  storageClassName: local-path
  storageClassNameEnabled: true


suppcheck:
  ingress:
    host: suppcheck-dev.biester.vip
    tlsEnabled: true
mongodb:
  host: mongodb-service.treasury.svc.cluster.local
  auth: false